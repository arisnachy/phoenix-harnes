# Explicit session-query tool opt-in for the dedicated spill scenario. The
# nested filesystem overlay supplies private spill storage and its byte limit.
- id: base
  name: '@phoenix-ai/cordis-plugin-include'
  config:
    path: ./fs.cordis.yml
    patches:
      - insert:
          - id: tool-session-query
            name: '@phoenix-ai/dsh-tool-session-query'
          - id: timeout-policy
            name: '@phoenix-ai/dsh-tool-call-timeout-policy'
