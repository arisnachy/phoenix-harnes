# Keyless replay keeps the LSP composition intact and replaces only the model adapter.
- id: base
  name: '@deepseek-ai/cordis-plugin-include'
  config:
    path: ../cordis.yml
    patches:
      - id: llm-deepseek
        name: '@phoenix-ai/dsh-llm-deepseek'
        disabled: true
      - insert:
          - id: lsp
            name: '@phoenix-ai/dsh-lsp'
          - id: lsp-stdio
            name: '@phoenix-ai/dsh-lsp-stdio'
            config:
              servers:
                fixture:
                  command: !!js process.execPath
                  args: ['./lsp-server.mjs']
                  extensionToLanguage:
                    '.ts': typescript
          - id: timeout-policy
            name: '@phoenix-ai/dsh-tool-call-timeout-policy'
          - id: tool-lsp
            name: '@phoenix-ai/dsh-tool-lsp'
            config:
              maxLocations: 1
          - id: llm-replay
            name: '@phoenix-ai/dsh-llm-replay'
