name: Temporary quality task1 verification

on:
  push:
    branches:
      - feat/quality-foresight-completion-20260915
    paths:
      - .github/workflows/tmp-quality-task1.yml

permissions:
  contents: read

jobs:
  verify-task1:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v4
        with:
          version: 11.7.0
      - uses: actions/setup-node@v6
        with:
          node-version: 24
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec vitest run packages/quality/quality/tests/readiness.spec.ts
      - run: pnpm exec tsc -b packages/quality/quality/tsconfig.json
